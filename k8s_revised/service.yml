apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: my-namespace
spec:
  selector:
    name: nginx-deployment
    labels:
      app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
