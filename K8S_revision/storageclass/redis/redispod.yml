apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: task-pv-volume
      mountPath: /usr/share/nginx/html

  volumes:
  - name: task-pv-volume
    persistentVolumeClaim:
      claimName: task-pv-claim