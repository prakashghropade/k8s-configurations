kind: Service
apiVersion: v1
metadata:
  name: nginx-service
  labels:
    app: nginx-service
    env: dev
spec:
  type: NodePort
  selector:
      app: nginx-app
      env: dev
  ports:
    - name: nginx-service
      nodePort: 30001
      port: 80
      targetPort: 80
