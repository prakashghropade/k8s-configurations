kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  labels:
    app: nginx-app
    env: dev
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    ports:
    - containerPort: 80
  nodeSelector:
    gpu: "false"
